import { AlertDashboardDto } from './alert-dashboard-dto.model';

describe('AlertDashboardDto', () => {
  it('should create an instance', () => {
    expect(new AlertDashboardDto()).toBeTruthy();
  });
});
