import { GatewaysCountDo } from './gateways-count-do.model';

describe('GatewaysCountDo.Model', () => {
  it('should create an instance', () => {
    expect(new GatewaysCountDo()).toBeTruthy();
  });
});
