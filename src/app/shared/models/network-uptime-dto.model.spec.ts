import { NetworkUptimeDto } from './network-uptime-dto.model';

describe('NetworkUptimeDto.Model', () => {
  it('should create an instance', () => {
    expect(new NetworkUptimeDto()).toBeTruthy();
  });
});
